# ADR-0005：第一版不引入 Redis

- 状态：Accepted
- 日期：2026-07-17

## 决策

第一版仅使用 PostgreSQL，不部署 Redis。

## 原因

系统按 50 个账号、50 条并发流设计，不限流、不做响应缓存，Redis 没有足够收益。PostgreSQL 可以承担任务锁、告警去重、会话和核心数据。

## 后果

- 部署与备份更简单。
- Gateway 热路径使用版本化只读路由快照，避免同步数据库阻塞。
- 未来实现响应缓存或大规模多实例时可通过 SPI 引入 Redis。

